apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "trip-project.fullname" . }}-test-connection"
  labels:
    {{- include "trip-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "trip-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
