apiVersion: v1
kind: Service
metadata:
  name: {{ include "trip-project.fullname" . }}
  labels:
    {{- include "trip-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trip-project.selectorLabels" . | nindent 4 }}
